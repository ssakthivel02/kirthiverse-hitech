name: KirthiVerse Manus Preview

on:
  push:
    branches:
      - design/manus-visual-master
  workflow_dispatch:

permissions:
  contents: read

env:
  RAW_CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
  RAW_CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
  PREVIEW_URL: https://kirthiverse-hitech.ssakthivel02.workers.dev

jobs:
  validate-and-preview:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout visual candidate
        uses: actions/checkout@v4

      - name: Validate candidate contract
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import json
          from pathlib import Path

          index = Path('index.html').read_text(encoding='utf-8')
          foundation = Path('learning-foundation-v19.js').read_text(encoding='utf-8')
          tools = Path('learning-tools-v20.js').read_text(encoding='utf-8')
          nav = Path('navigation-v18.js').read_text(encoding='utf-8')
          lifecycle = Path('runtime-lifecycle-v16.js').read_text(encoding='utf-8')
          runtime = json.loads(Path('preview-runtime.json').read_text(encoding='utf-8'))
          preview = json.loads(Path('visual-preview.json').read_text(encoding='utf-8'))

          required_index = [
              'MANUS-VISUAL-MASTER-05', 'navigation-v18.js',
              'learning-foundation-v19.js', 'learning-foundation-v19.css',
              'learning-tools-v20.js', 'learning-tools-v20.css'
          ]
          for marker in required_index:
              assert marker in index, f'missing index marker: {marker}'

          legacy = [
              'experience.js','intelligence-v4.js','cockpit-v5.js',
              'experience.css','intelligence-v4.css','cockpit-v5.css',
              'mastery-v6.js','workspace-v7.js','planner-v8.js',
              'mastery-v6.css','workspace-v7.css','planner-v8.css',
              'diagnostics-v9.js','release-v10.js','gate-v11.js',
              'rc-v12.js','closure-v13.js','preprod-v14.js'
          ]
          assert not any(x in index for x in legacy), 'legacy runtime/style reference returned to index.html'

          assert 'KV_LEARNING_FOUNDATION' in foundation
          for marker in ['experience','intelligence','cockpit']:
              assert marker in foundation, f'foundation module marker missing: {marker}'

          assert 'KV_LEARNING_TOOLS' in tools
          for marker in [
              'mastery','workspace','planner',
              'kirthiverse.hitech.static.progress.v2',
              'kirthiverse.hitech.workspace.v1',
              'kirthiverse.hitech.planner.v8',
              'BY_ID','ASSESS_BY_LESSON'
          ]:
              assert marker in tools, f'learning tools marker missing: {marker}'
          assert "addEventListener('popstate'" not in tools

          assert 'delegated-capture' in nav
          assert 'agePathCentralized:true' in nav
          assert "source:'router-popstate-after-app-render'" in lifecycle
          assert "observerScope:'none'" in lifecycle
          assert 'new MutationObserver' not in lifecycle
          assert runtime.get('lifecycleObserver') is False

          bundles = runtime.get('bundles') or {}
          foundation_contract = bundles.get('learnerFoundation') or {}
          tools_contract = bundles.get('learningTools') or {}
          assert foundation_contract.get('javascript') == 'learning-foundation-v19.js'
          assert foundation_contract.get('stylesheet') == 'learning-foundation-v19.css'
          assert tools_contract.get('javascript') == 'learning-tools-v20.js'
          assert tools_contract.get('stylesheet') == 'learning-tools-v20.css'
          assert tools_contract.get('browserRequestsBefore') == 6
          assert tools_contract.get('browserRequestsAfter') == 2
          assert tools_contract.get('sharedHelpers') is True
          assert tools_contract.get('sharedRenderScheduler') is True
          assert tools_contract.get('indexedLessonLookup') is True
          assert tools_contract.get('originalFilesLoadedByIndex') is False

          canonical = preview.get('canonical') or {}
          assert canonical.get('lessons') == 135
          assert canonical.get('assessments') == 72
          assert canonical.get('worlds') == 11
          assert preview.get('productionCutoverAllowed') is False
          assert preview.get('mergeAllowed') is False

          contamination = ['__manus__','manus-storage','vite-plugin-manus-runtime','BUILT_IN_FORGE','artifacts.cloudflare.net','ManusDialog']
          loaded_runtime = '\n'.join(Path(p).read_text(encoding='utf-8') for p in [
              'app.js','navigation-v18.js','runtime-lifecycle-v16.js',
              'learning-foundation-v19.js','learning-tools-v20.js',
              'visual-master-v15.js','profile-v17.js','visual-master-pages-v15.js'
          ])
          assert not any(x in loaded_runtime for x in contamination), 'Manus/runtime contamination detected'
          print('Candidate contract validated structurally.')
          PY

      - name: Measure current runtime payload
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import os, re
          from pathlib import Path

          baseline = 159428
          html = Path('index.html').read_text(encoding='utf-8')
          refs = re.findall(r'(?:href|src)=["\'](/[^"\']+\.(?:css|js)(?:\?[^"\']*)?)["\']', html)
          assets=[]
          for ref in refs:
              path=ref.split('?',1)[0].lstrip('/')
              if path.startswith('data/'):
                  continue
              if path not in assets:
                  assets.append(path)
          missing=[p for p in assets if not Path(p).is_file()]
          if missing:
              raise SystemExit(f'Missing referenced assets: {missing}')
          css=[p for p in assets if p.endswith('.css')]
          js=[p for p in assets if p.endswith('.js')]
          css_bytes=sum(Path(p).stat().st_size for p in css)
          js_bytes=sum(Path(p).stat().st_size for p in js)
          total=css_bytes+js_bytes
          reduction=baseline-total
          pct=(reduction/baseline*100) if baseline else 0
          target=round(baseline*0.75)

          print(f'PAYLOAD_CSS_BYTES={css_bytes}')
          print(f'PAYLOAD_JS_BYTES={js_bytes}')
          print(f'PAYLOAD_COMBINED_BYTES={total}')
          print(f'PAYLOAD_REDUCTION_BYTES={reduction}')
          print(f'PAYLOAD_REDUCTION_PERCENT={pct:.2f}')
          print(f'PAYLOAD_25_PERCENT_TARGET_BYTES={target}')
          print('PAYLOAD_CSS_ASSETS='+','.join(css))
          print('PAYLOAD_JS_ASSETS='+','.join(js))

          summary=os.environ['GITHUB_STEP_SUMMARY']
          with open(summary,'a',encoding='utf-8') as f:
              f.write('### Runtime payload measurement\n')
              f.write(f'- Baseline combined: {baseline:,} bytes\n')
              f.write(f'- Current CSS: {css_bytes:,} bytes\n')
              f.write(f'- Current JavaScript: {js_bytes:,} bytes\n')
              f.write(f'- Current combined: {total:,} bytes\n')
              f.write(f'- Reduction: {reduction:,} bytes ({pct:.2f}%)\n')
              f.write(f'- 25% target threshold: {target:,} bytes\n')
              f.write(f'- Target met: {"YES" if total <= target else "NO"}\n')
          PY

      - name: Confirm Worker target
        shell: bash
        run: |
          set -euo pipefail
          grep -q '"name": "kirthiverse-hitech"' wrangler.jsonc
          grep -q '"directory": "."' wrangler.jsonc
          echo 'Existing kirthiverse-hitech Worker target confirmed.'

      - name: Normalize Cloudflare credential formatting
        if: ${{ env.RAW_CLOUDFLARE_API_TOKEN != '' && env.RAW_CLOUDFLARE_ACCOUNT_ID != '' }}
        shell: bash
        run: |
          set -euo pipefail
          python - <<'PY'
          import os
          token=os.environ.get('RAW_CLOUDFLARE_API_TOKEN','').strip().strip('"').strip("'")
          if token.lower().startswith('bearer '):
              token=token[7:].strip()
          account=os.environ.get('RAW_CLOUDFLARE_ACCOUNT_ID','').strip().strip('"').strip("'")
          if not token or not account:
              raise SystemExit('Normalized Cloudflare credential is empty.')
          with open(os.environ['GITHUB_ENV'],'a',encoding='utf-8') as f:
              f.write('CLOUDFLARE_API_TOKEN_CLEAN='+token+'\n')
              f.write('CLOUDFLARE_ACCOUNT_ID_CLEAN='+account+'\n')
          PY
          echo 'Cloudflare credential formatting normalized without printing secret values.'

      - name: Deploy controlled preview to existing Worker
        if: ${{ env.RAW_CLOUDFLARE_API_TOKEN != '' && env.RAW_CLOUDFLARE_ACCOUNT_ID != '' }}
        env:
          CLOUDFLARE_API_TOKEN: ${{ env.CLOUDFLARE_API_TOKEN_CLEAN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ env.CLOUDFLARE_ACCOUNT_ID_CLEAN }}
        run: npx --yes wrangler@4 deploy --assets=.

      - name: Post-deploy HTTP smoke gate
        if: ${{ env.RAW_CLOUDFLARE_API_TOKEN != '' && env.RAW_CLOUDFLARE_ACCOUNT_ID != '' }}
        shell: bash
        run: |
          set -euo pipefail
          sleep 4

          fetch() {
            local label="$1" url="$2" out="$3"
            echo "SMOKE_FETCH_START: $label"
            code=$(curl --silent --show-error --location --retry 4 --retry-delay 2 --output "$out" --write-out '%{http_code}' "$url")
            echo "SMOKE_HTTP: $label=$code"
            test "$code" = "200"
            test -s "$out"
          }

          expect_marker() {
            local label="$1" file="$2" marker="$3"
            if grep -Fq "$marker" "$file"; then
              echo "SMOKE_ASSERT_PASS: $label"
            else
              echo "SMOKE_ASSERT_FAIL: $label"
              exit 1
            fi
          }

          fetch 'home' "$PREVIEW_URL/" /tmp/home.html
          expect_marker 'home candidate marker' /tmp/home.html 'MANUS-VISUAL-MASTER-05'
          expect_marker 'home learner foundation js' /tmp/home.html 'learning-foundation-v19.js'
          expect_marker 'home learner foundation css' /tmp/home.html 'learning-foundation-v19.css'
          expect_marker 'home learning tools js' /tmp/home.html 'learning-tools-v20.js'
          expect_marker 'home learning tools css' /tmp/home.html 'learning-tools-v20.css'
          if grep -Eq 'experience\.js|intelligence-v4\.js|cockpit-v5\.js|experience\.css|intelligence-v4\.css|cockpit-v5\.css|mastery-v6\.js|workspace-v7\.js|planner-v8\.js|mastery-v6\.css|workspace-v7\.css|planner-v8\.css' /tmp/home.html; then
            echo 'SMOKE_ASSERT_FAIL: legacy learner assets still referenced'
            exit 1
          fi
          echo 'SMOKE_ASSERT_PASS: legacy learner assets not referenced'

          fetch 'learner foundation js' "$PREVIEW_URL/learning-foundation-v19.js" /tmp/foundation.js
          expect_marker 'learner foundation runtime marker' /tmp/foundation.js 'KV_LEARNING_FOUNDATION'
          fetch 'learner foundation css' "$PREVIEW_URL/learning-foundation-v19.css" /tmp/foundation.css
          expect_marker 'learner foundation style marker' /tmp/foundation.css 'learner foundation v19'
          fetch 'learning tools js' "$PREVIEW_URL/learning-tools-v20.js" /tmp/tools.js
          expect_marker 'learning tools runtime marker' /tmp/tools.js 'KV_LEARNING_TOOLS'
          fetch 'learning tools css' "$PREVIEW_URL/learning-tools-v20.css" /tmp/tools.css
          expect_marker 'learning tools style marker' /tmp/tools.css 'learning tools v20'

          fetch 'preview runtime manifest' "$PREVIEW_URL/preview-runtime.json" /tmp/runtime.json
          python - <<'PY'
          import json
          with open('/tmp/runtime.json', encoding='utf-8') as f:
              data=json.load(f)
          bundles=data.get('bundles') or {}
          fnd=bundles.get('learnerFoundation') or {}
          tools=bundles.get('learningTools') or {}
          assert fnd.get('javascript') == 'learning-foundation-v19.js', fnd
          assert fnd.get('stylesheet') == 'learning-foundation-v19.css', fnd
          assert tools.get('javascript') == 'learning-tools-v20.js', tools
          assert tools.get('stylesheet') == 'learning-tools-v20.css', tools
          assert tools.get('browserRequestsBefore') == 6, tools
          assert tools.get('browserRequestsAfter') == 2, tools
          assert tools.get('sharedHelpers') is True, tools
          assert tools.get('sharedRenderScheduler') is True, tools
          assert tools.get('indexedLessonLookup') is True, tools
          assert tools.get('originalFilesLoadedByIndex') is False, tools
          print('SMOKE_ASSERT_PASS: runtime bundle contract')
          PY

          fetch 'preview contract' "$PREVIEW_URL/visual-preview.json" /tmp/preview.json
          python - <<'PY'
          import json
          with open('/tmp/preview.json', encoding='utf-8') as f:
              data=json.load(f)
          assert data.get('candidate') == 'MANUS-VISUAL-MASTER-05'
          canonical=data.get('canonical') or {}
          assert canonical.get('lessons') == 135
          assert canonical.get('assessments') == 72
          assert canonical.get('worlds') == 11
          assert data.get('productionCutoverAllowed') is False
          assert data.get('mergeAllowed') is False
          print('SMOKE_ASSERT_PASS: preview contract structure')
          PY

          fetch 'profile deep link' "$PREVIEW_URL/profile" /tmp/profile.html
          expect_marker 'profile SPA shell' /tmp/profile.html 'MANUS-VISUAL-MASTER-05'

          fetch 'lesson deep link' "$PREVIEW_URL/lesson/math.number.place-value.base10.age6-8.l1" /tmp/lesson.html
          expect_marker 'lesson SPA shell' /tmp/lesson.html 'MANUS-VISUAL-MASTER-05'

          fetch 'canonical lesson asset' "$PREVIEW_URL/data/lessons-1.js" /tmp/lessons.js
          expect_marker 'lesson data payload' /tmp/lessons.js 'KV_LESSONS'

          echo '### Browser-entry smoke gate PASS' >> "$GITHUB_STEP_SUMMARY"
          echo '- Home candidate marker: PASS' >> "$GITHUB_STEP_SUMMARY"
          echo '- Learner foundation v19 bundle: PASS' >> "$GITHUB_STEP_SUMMARY"
          echo '- Learning tools v20 bundle: PASS' >> "$GITHUB_STEP_SUMMARY"
          echo '- Legacy learner references: ABSENT' >> "$GITHUB_STEP_SUMMARY"
          echo '- Runtime bundle manifest: PASS' >> "$GITHUB_STEP_SUMMARY"
          echo '- Preview contract structure: PASS' >> "$GITHUB_STEP_SUMMARY"
          echo '- Profile deep link: PASS' >> "$GITHUB_STEP_SUMMARY"
          echo '- Lesson deep link: PASS' >> "$GITHUB_STEP_SUMMARY"
          echo '- Canonical lesson asset: PASS' >> "$GITHUB_STEP_SUMMARY"

      - name: Deployment credential status
        if: ${{ env.RAW_CLOUDFLARE_API_TOKEN == '' || env.RAW_CLOUDFLARE_ACCOUNT_ID == '' }}
        shell: bash
        run: |
          echo '### Cloudflare preview deployment not executed' >> "$GITHUB_STEP_SUMMARY"
          echo 'Validation passed, but Cloudflare repository credentials are not configured.' >> "$GITHUB_STEP_SUMMARY"
          echo 'No production DNS or Cloudflare project was changed.' >> "$GITHUB_STEP_SUMMARY"

      - name: Preview safety summary
        if: always()
        shell: bash
        run: |
          echo '### KirthiVerse controlled preview' >> "$GITHUB_STEP_SUMMARY"
          echo '- Branch: design/manus-visual-master' >> "$GITHUB_STEP_SUMMARY"
          echo '- Candidate: MANUS-VISUAL-MASTER-05' >> "$GITHUB_STEP_SUMMARY"
          echo '- Navigation: centralized delegated SPA controller' >> "$GITHUB_STEP_SUMMARY"
          echo '- Render lifecycle: observer-free router events' >> "$GITHUB_STEP_SUMMARY"
          echo '- Learner foundation: Experience + Intelligence + Cockpit bundled' >> "$GITHUB_STEP_SUMMARY"
          echo '- Learning tools: Mastery + Workspace + Planner bundled and shared-runtime optimized' >> "$GITHUB_STEP_SUMMARY"
          echo '- Worker target: kirthiverse-hitech' >> "$GITHUB_STEP_SUMMARY"
          echo '- Production custom-domain configuration: unchanged by this workflow' >> "$GITHUB_STEP_SUMMARY"
          echo '- Paid Cloudflare services: none enabled' >> "$GITHUB_STEP_SUMMARY"
