import assert from 'node:assert/strict';
import fs from 'node:fs';
import vm from 'node:vm';
const lessonIds=['math.cbse6.ganita-prakash.ch7.fractional-units-number-line.v1','math.cbse6.ganita-prakash.ch7.equivalence-comparison.v1','math.cbse6.ganita-prakash.ch7.add-subtract.v1'];
const map=JSON.parse(fs.readFileSync('docs/class6-pilot/MATHEMATICS_GANITA_PRAKASH_MAP_V1.json','utf8'));assert.equal(map.schemaVersion,'1.10.0');assert.equal(map.chapters[6].chapter,7);assert.equal(map.chapters[6].title,'Fractions');assert.equal(map.chapters[6].status,'KIKI_TEACHING_SLICE_COMPLETE');assert.equal(map.chapter7Topics.length,3);assert.ok(map.chapter7Topics.every(x=>x.status==='END_TO_END_PILOT_IMPLEMENTED'));assert.equal(map.chapter7CompletionEvidence.lessonCount,3);assert.equal(map.chapter7CompletionEvidence.assessmentCount,15);assert.equal(map.chapter7CompletionEvidence.schoolNeedsValidationPresent,true);
const sandbox={window:{KV_LESSONS:[],KV_ASSESSMENTS:[]}};vm.createContext(sandbox);vm.runInContext(fs.readFileSync('data/class6-math-ch7.js','utf8'),sandbox);vm.runInContext(fs.readFileSync('data/class6-math-ch7-assessments.js','utf8'),sandbox);const lessons=sandbox.window.KV_LESSONS,assessments=sandbox.window.KV_ASSESSMENTS;assert.equal(lessons.length,3);assert.equal(assessments.length,15);for(const lessonId of lessonIds)assert.equal(assessments.filter(x=>x.lessonId===lessonId).length,5);
const allText=JSON.stringify({lessons,assessments,map:map.chapter7Topics}).toLowerCase();for(const required of ['equal parts','fractional unit','number line','mixed number','improper fraction','equivalent fraction','common denominator','addition','subtraction'])assert.ok(allText.includes(required));
const runtimeSandbox={window:{KV_LESSONS:[]}};vm.createContext(runtimeSandbox);vm.runInContext(fs.readFileSync('data/class6-math-ch3-4-runtime.js','utf8'),runtimeSandbox);assert.equal(runtimeSandbox.window.KV_LESSONS.length,15);assert.ok(lessonIds.every(id=>runtimeSandbox.window.KV_LESSONS.some(x=>x.id===id)));
const entry=fs.readFileSync('p0-entry-v1.js','utf8');for(const asset of ['data/class6-math-ch7-assessments.js','data/class6-math-ch9-assessments.js'])assert.ok(entry.includes(asset));assert.ok(entry.includes('ch(?:2|3|4|5|6|7|8|9)\\.'));assert.ok(!entry.includes("/^\\/lesson\\/math\\./"));const baseChunk=entry.slice(entry.indexOf('const mathBaseFiles'),entry.indexOf('const loadClass6MathPilot'));assert.ok(!baseChunk.includes('data/class6-math-ch9.js'));assert.ok(!baseChunk.includes('data/class6-math-ch9-assessments.js'));
const sw=fs.readFileSync('sw-v30.js','utf8');for(const asset of ['/data/class6-math-ch7-assessments.js','/data/class6-math-ch9-assessments.js'])assert.ok(sw.includes(asset));
console.log(`CLASS6_MATH_CH7_PASS topics=${map.chapter7Topics.length} lessons=${lessons.length} assessments=${assessments.length} schoolNeeds=${map.chapter7CompletionEvidence.schoolNeedsValidationPresent}`);