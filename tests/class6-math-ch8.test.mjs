import assert from 'node:assert/strict';
import fs from 'node:fs';
import vm from 'node:vm';
const lessonIds=['math.cbse6.ganita-prakash.ch8.compass-circles-arcs.v1','math.cbse6.ganita-prakash.ch8.square-rectangle-properties.v1','math.cbse6.ganita-prakash.ch8.construct-squares-rectangles.v1'];
const map=JSON.parse(fs.readFileSync('docs/class6-pilot/MATHEMATICS_GANITA_PRAKASH_MAP_V1.json','utf8'));assert.equal(map.schemaVersion,'1.10.0');assert.equal(map.chapters[7].chapter,8);assert.equal(map.chapters[7].title,'Playing with Constructions');assert.equal(map.chapters[7].status,'KIKI_TEACHING_SLICE_COMPLETE');assert.equal(map.chapter8Topics.length,3);assert.ok(map.chapter8Topics.every(x=>x.status==='END_TO_END_PILOT_IMPLEMENTED'));assert.equal(map.chapter8CompletionEvidence.lessonCount,3);assert.equal(map.chapter8CompletionEvidence.assessmentCount,15);assert.equal(map.chapter8CompletionEvidence.schoolNeedsValidationPresent,true);assert.equal(map.chapter8CompletionEvidence.schoolNeedsValidationArtifact,'docs/class6-pilot/MATH_CH8_SCHOOL_NEEDS_VALIDATION_V1.json');
const sandbox={window:{KV_LESSONS:[],KV_ASSESSMENTS:[]}};vm.createContext(sandbox);vm.runInContext(fs.readFileSync('data/class6-math-ch8.js','utf8'),sandbox);vm.runInContext(fs.readFileSync('data/class6-math-ch8-assessments.js','utf8'),sandbox);const lessons=sandbox.window.KV_LESSONS,assessments=sandbox.window.KV_ASSESSMENTS;assert.equal(lessons.length,3);assert.equal(assessments.length,15);for(const lessonId of lessonIds)assert.equal(assessments.filter(x=>x.lessonId===lessonId).length,5);
const carrierSandbox={window:{KV_LESSONS:[]}};vm.createContext(carrierSandbox);vm.runInContext(fs.readFileSync('data/class6-math-ch2.js','utf8'),carrierSandbox);const runtimeChapter8=carrierSandbox.window.KV_LESSONS.filter(x=>x.chapter===8);assert.equal(runtimeChapter8.length,3);const sourceById=new Map(lessons.map(x=>[x.id,x]));for(const runtimeLesson of runtimeChapter8)assert.deepEqual(JSON.parse(JSON.stringify(runtimeLesson)),JSON.parse(JSON.stringify(sourceById.get(runtimeLesson.id))));
const entry=fs.readFileSync('p0-entry-v1.js','utf8');for(const asset of ['data/class6-math-ch8-assessments.js','data/class6-math-ch9-assessments.js'])assert.ok(entry.includes(asset));assert.ok(entry.includes('ch(?:2|3|4|5|6|7|8|9)\\.'));assert.ok(!entry.includes("/^\\/lesson\\/math\\./"));const baseChunk=entry.slice(entry.indexOf('const mathBaseFiles'),entry.indexOf('const loadClass6MathPilot'));assert.ok(!baseChunk.includes('data/class6-math-ch8.js'));assert.ok(!baseChunk.includes('data/class6-math-ch9.js'));assert.ok(!baseChunk.includes('data/class6-math-ch8-assessments.js'));assert.ok(!baseChunk.includes('data/class6-math-ch9-assessments.js'));
const sw=fs.readFileSync('sw-v30.js','utf8');for(const asset of ['/data/class6-math-ch8-assessments.js','/data/class6-math-ch9-assessments.js'])assert.ok(sw.includes(asset));
console.log(`CLASS6_MATH_CH8_PASS topics=${map.chapter8Topics.length} lessons=${lessons.length} assessments=${assessments.length} runtimeParity=${runtimeChapter8.length} schoolNeeds=${map.chapter8CompletionEvidence.schoolNeedsValidationPresent}`);